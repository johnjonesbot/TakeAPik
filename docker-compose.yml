services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: takeapik
      POSTGRES_PASSWORD: takeapik-dev
      POSTGRES_DB: takeapik
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U takeapik"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
